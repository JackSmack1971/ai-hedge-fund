---
phase: 01-foundation-schemas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/hybrid.py
  - tests/schemas/__init__.py
  - tests/schemas/test_hybrid.py
autonomous: true
requirements:
  - SCHM-01

must_haves:
  truths:
    - "All 7 Pydantic models in src/schemas/hybrid.py import without error"
    - "Extra JSON fields passed to any hybrid model are silently dropped (not raising ValidationError)"
    - "HybridDecisionTrace serializes a full trace including timestamp and reasoning_summary"
    - "All schema tests pass (existing 36 + new tests for D-12/D-13)"
    - "The full pytest suite (417+ tests) remains green after changes"
  artifacts:
    - path: "src/schemas/hybrid.py"
      provides: "All 7 hybrid Pydantic models with ConfigDict(extra='ignore') and complete HybridDecisionTrace"
      contains: "model_config = ConfigDict(extra=\"ignore\")"
    - path: "tests/schemas/__init__.py"
      provides: "pytest discovery marker for schemas test package"
    - path: "tests/schemas/test_hybrid.py"
      provides: "36 existing + new tests covering D-12 and D-13"
      contains: "test_extra_fields_ignored"
  key_links:
    - from: "tests/schemas/test_hybrid.py"
      to: "src/schemas/hybrid.py"
      via: "import HybridDecisionTrace, RegimeClassification, etc."
      pattern: "from src\\.schemas\\.hybrid import"
---

<objective>
Patch the existing `src/schemas/hybrid.py` to satisfy all locked decisions from CONTEXT.md that are currently unmet (D-12: extra field tolerance, D-13: missing HybridDecisionTrace fields), then verify all schema tests pass.

Purpose: SCHM-01 is 95% done. The schema file already has 7 correct models and 36 passing tests. Only two gaps remain: `ConfigDict(extra="ignore")` is absent from all models (violating D-12), and `HybridDecisionTrace` is missing `timestamp` and `reasoning_summary` fields (violating D-13). This plan closes both gaps without touching any agent code.

Output: Patched `src/schemas/hybrid.py`; `tests/schemas/__init__.py`; new D-12 and D-13 test coverage added to `tests/schemas/test_hybrid.py`.
</objective>

<execution_context>
@C:/Users/click/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/click/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-schemas/01-CONTEXT.md
@.planning/phases/01-foundation-schemas/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Patch hybrid.py — add ConfigDict(extra="ignore") to all models and add missing HybridDecisionTrace fields</name>
  <files>src/schemas/hybrid.py</files>
  <read_first>
    - src/schemas/hybrid.py — read the FULL file first; understand current state of all 7 models and the HybridDecisionTrace definition before touching anything
    - .planning/phases/01-foundation-schemas/01-CONTEXT.md — locked decisions D-12, D-13, D-14, D-15
  </read_first>
  <behavior>
    - Test 1 (D-12): `RegimeClassification.model_validate({"regime": "momentum", "confidence": 80, "reasoning": "x", "unexpected_field": "ignored"})` does NOT raise ValidationError; the resulting object has no `unexpected_field` attribute
    - Test 2 (D-12): Same extra-field tolerance applies for AgentSelection, ThesisOutput, DebateOutput, GuardrailOutput, MetaLabelOutput, HybridDecisionTrace
    - Test 3 (D-13): `HybridDecisionTrace(ticker="AAPL").timestamp` is None (field exists, defaults None)
    - Test 4 (D-13): `HybridDecisionTrace(ticker="AAPL").reasoning_summary` is None (field exists, defaults None)
    - Test 5 (D-13): A full trace with all fields including timestamp and reasoning_summary round-trips through model_dump() and model_validate()
    - Test 6: All previously passing 36 schema tests still pass (no regression)
  </behavior>
  <action>
    Read src/schemas/hybrid.py fully. Then make these targeted changes only — do NOT rewrite models that are already correct:

    1. Add `ConfigDict` to the pydantic import: change `from pydantic import BaseModel, Field` to `from pydantic import BaseModel, ConfigDict, Field`.

    2. Add `from datetime import datetime` to the stdlib imports at the top.

    3. For EVERY class that extends BaseModel (all 7: RegimeClassification, AgentSelection, ThesisOutput, DebateOutput, GuardrailOutput, MetaLabelOutput, HybridDecisionTrace), add as the FIRST line of the class body: `model_config = ConfigDict(extra="ignore")` — per D-12.

    4. In HybridDecisionTrace specifically, add two new fields (per D-13, D-14, D-15):
       - `timestamp: datetime | None = None` — insert after the `ticker` field
       - `reasoning_summary: str | None = None` — insert as the last field

    5. Do NOT change any existing field definitions, constraints, or descriptions. The existing 36 tests must not break.

    6. Keep all JSON keys in snake_case per D-15 (already the case; confirm no alias is introduced).
  </action>
  <verify>
    <automated>cd "C:/workspaces/ai-hedge-fund-forked" && poetry run pytest tests/schemas/ -q --no-cov 2>&1 | tail -5</automated>
  </verify>
  <done>All tests in tests/schemas/ pass. `grep -c "model_config = ConfigDict" src/schemas/hybrid.py` returns 7. `grep -c "timestamp" src/schemas/hybrid.py` returns at least 1 (the field). `grep -c "reasoning_summary" src/schemas/hybrid.py` returns at least 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/schemas/__init__.py and add D-12/D-13 test coverage</name>
  <files>tests/schemas/__init__.py, tests/schemas/test_hybrid.py</files>
  <read_first>
    - tests/schemas/test_hybrid.py — read the full existing test file; understand class structure and existing test cases before adding new ones
    - src/schemas/hybrid.py — read to confirm actual field names and ConfigDict placement after Task 1
  </read_first>
  <behavior>
    - Test D-12a: `RegimeClassification.model_validate({"regime": "risk_on", "confidence": 50, "reasoning": "t", "extra_key": "extra_value"})` succeeds and the result does not have an `extra_key` attribute
    - Test D-12b: `HybridDecisionTrace.model_validate({"ticker": "AAPL", "unknown_llm_field": "some_value"})` succeeds
    - Test D-13a: `HybridDecisionTrace(ticker="AAPL").timestamp is None` — field present and defaults None
    - Test D-13b: `HybridDecisionTrace(ticker="AAPL").reasoning_summary is None` — field present and defaults None
    - Test D-13c: A HybridDecisionTrace constructed with `timestamp=datetime(2026, 1, 1, 12, 0, 0)` and `reasoning_summary="Bull case dominates"` round-trips correctly through model_dump() — dump contains both fields with expected values
    - Regression: all 36 original tests continue to pass
  </behavior>
  <action>
    1. Create `tests/schemas/__init__.py` as an empty file (required for pytest package discovery; the tests/schemas/ directory already exists but lacks this file).

    2. Open `tests/schemas/test_hybrid.py` and append a new test class `TestDecisionD12ExtraFields` with two test methods:
       - `test_extra_fields_ignored_on_regime_classification`: validates RegimeClassification with an extra "bogus_field" key; asserts no ValidationError; asserts `hasattr(result, "bogus_field")` is False
       - `test_extra_fields_ignored_on_hybrid_decision_trace`: validates HybridDecisionTrace with ticker="AAPL" and extra key "llm_noise"="value"; asserts no ValidationError

    3. Append a new test class `TestDecisionD13TraceFields` with three test methods:
       - `test_timestamp_field_defaults_none`: creates `HybridDecisionTrace(ticker="AAPL")`; asserts `hdt.timestamp is None`
       - `test_reasoning_summary_defaults_none`: creates `HybridDecisionTrace(ticker="AAPL")`; asserts `hdt.reasoning_summary is None`
       - `test_full_trace_with_timestamp_and_reasoning`: imports datetime, constructs HybridDecisionTrace with `ticker="AAPL"`, `timestamp=datetime(2026, 1, 1, 12, 0, 0)`, `reasoning_summary="Bull case is stronger"`, calls `model_dump()`, asserts `dump["timestamp"]` is not None and `dump["reasoning_summary"] == "Bull case is stronger"`

    4. Add `from datetime import datetime` to the imports in test_hybrid.py (it is not currently imported).

    Do NOT modify any existing test class or test method.
  </action>
  <verify>
    <automated>cd "C:/workspaces/ai-hedge-fund-forked" && poetry run pytest tests/schemas/ -v --no-cov 2>&1 | tail -20</automated>
  </verify>
  <done>pytest reports all tests in tests/schemas/ collected and passing. Count includes the original 36 plus the 5 new tests. No "no tests ran" or import errors.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression — verify baseline test suite still passes</name>
  <files></files>
  <read_first>
    - No file read needed; this is a pure verification step
  </read_first>
  <action>
    Run the full pytest suite. The schema changes (adding ConfigDict and two new fields with None defaults) must not break any of the 417+ baseline tests. If failures occur, inspect output: likely a test imports `HybridDecisionTrace` and checks exact field count — fix by reading that test and adjusting the assertion to include the new fields.
  </action>
  <verify>
    <automated>cd "C:/workspaces/ai-hedge-fund-forked" && poetry run pytest -q --no-cov 2>&1 | tail -10</automated>
  </verify>
  <done>Full suite reports 0 failures. The count of passed tests is >= 417 (the pre-phase baseline). The line "passed" appears with no "failed" or "error" count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema parse boundary | Pydantic models parse LLM-generated JSON dicts in later phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | src/schemas/hybrid.py | accept | Pure data schema; no execution logic; Pydantic validates bounds at parse time via Field(ge=, le=) constraints already present |
| T-01-02 | Information Disclosure | extra fields silently dropped | accept | D-12 requires silent drop (not error); fields arriving from untrusted LLM output are not forwarded downstream — they vanish |
| T-01-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan; pydantic 2.13.4 already present and verified |
</threat_model>

<verification>
1. `grep -c "model_config = ConfigDict" src/schemas/hybrid.py` == 7 (one per class)
2. `grep "timestamp" src/schemas/hybrid.py` shows the field in HybridDecisionTrace
3. `grep "reasoning_summary" src/schemas/hybrid.py` shows the field in HybridDecisionTrace
4. `poetry run pytest tests/schemas/ -q --no-cov` — all pass (41+ tests)
5. `poetry run pytest -q --no-cov` — full suite green, >= 417 passed, 0 failed
</verification>

<success_criteria>
- SCHM-01 fully satisfied: all 7 Pydantic models in src/schemas/hybrid.py are importable, validate correctly, serialize to JSON, and silently drop extra fields
- D-12: Every model carries `model_config = ConfigDict(extra="ignore")`
- D-13: HybridDecisionTrace has `timestamp: datetime | None = None` and `reasoning_summary: str | None = None`
- D-14: Both new fields use `FieldType | None = None` pattern (nullable with default None)
- D-15: All keys remain snake_case — no aliases introduced
- Full test suite: 0 regressions
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File | Notes |
|--------|------|------|-------|
| `model_config = ConfigDict(extra="ignore")` | class attribute | `src/schemas/hybrid.py` | Added to all 7 BaseModel subclasses |
| `HybridDecisionTrace.timestamp` | field | `src/schemas/hybrid.py` | `datetime \| None = None` — D-13 |
| `HybridDecisionTrace.reasoning_summary` | field | `src/schemas/hybrid.py` | `str \| None = None` — D-13 |
| `tests/schemas/__init__.py` | file | `tests/schemas/__init__.py` | Empty; enables pytest package discovery |
| `TestDecisionD12ExtraFields` | test class | `tests/schemas/test_hybrid.py` | 2 tests covering D-12 |
| `TestDecisionD13TraceFields` | test class | `tests/schemas/test_hybrid.py` | 3 tests covering D-13 |

<output>
Create `.planning/phases/01-foundation-schemas/01-01-SUMMARY.md` when done.
</output>
