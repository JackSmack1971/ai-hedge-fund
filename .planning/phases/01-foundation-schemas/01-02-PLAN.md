---
phase: 01-foundation-schemas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/risk/__init__.py
  - src/risk/disagreement.py
  - src/risk/drawdown_guardrail.py
  - src/risk/sizing.py
  - tests/risk/__init__.py
  - tests/risk/test_disagreement.py
  - tests/risk/test_drawdown_guardrail.py
  - tests/risk/test_sizing.py
autonomous: true
requirements:
  - RISK-01
  - RISK-02
  - RISK-03

must_haves:
  truths:
    - "compute_disagreement_score returns 0.0 for unanimous buy signals and 1.0 for 50/50 buy/sell split"
    - "compute_disagreement_multiplier returns 1.0 - score (e.g. 0.0 for full disagreement, 1.0 for full consensus)"
    - "None stances are imputed as 0 (Hold) before computing standard deviation"
    - "compute_cppi_multiplier returns 0.0 when portfolio value is at or below the floor"
    - "compute_cppi_multiplier returns 1.0 when portfolio value equals peak value (full cushion)"
    - "fractional_kelly returns 1.0 (neutral) when enabled=False"
    - "fractional_kelly returns a value in [0.0, 0.25] when enabled=True"
    - "All three risk modules import with stdlib only (no numpy, no pandas, no LLM dependencies)"
    - "Full test suite (417+ tests) remains green after adding risk files"
  artifacts:
    - path: "src/risk/__init__.py"
      provides: "Package init for src/risk"
    - path: "src/risk/disagreement.py"
      provides: "compute_disagreement_score and compute_disagreement_multiplier pure functions"
      exports: ["compute_disagreement_score", "compute_disagreement_multiplier"]
    - path: "src/risk/drawdown_guardrail.py"
      provides: "compute_cppi_floor and compute_cppi_multiplier pure functions"
      exports: ["compute_cppi_floor", "compute_cppi_multiplier"]
    - path: "src/risk/sizing.py"
      provides: "fractional_kelly pure function"
      exports: ["fractional_kelly"]
    - path: "tests/risk/__init__.py"
      provides: "pytest discovery marker for risk test package"
    - path: "tests/risk/test_disagreement.py"
      provides: "Unit tests for RISK-01"
    - path: "tests/risk/test_drawdown_guardrail.py"
      provides: "Unit tests for RISK-02"
    - path: "tests/risk/test_sizing.py"
      provides: "Unit tests for RISK-03"
  key_links:
    - from: "tests/risk/test_disagreement.py"
      to: "src/risk/disagreement.py"
      via: "from src.risk.disagreement import compute_disagreement_score, compute_disagreement_multiplier"
      pattern: "from src\\.risk\\.disagreement import"
    - from: "tests/risk/test_drawdown_guardrail.py"
      to: "src/risk/drawdown_guardrail.py"
      via: "from src.risk.drawdown_guardrail import compute_cppi_floor, compute_cppi_multiplier"
      pattern: "from src\\.risk\\.drawdown_guardrail import"
    - from: "tests/risk/test_sizing.py"
      to: "src/risk/sizing.py"
      via: "from src.risk.sizing import fractional_kelly"
      pattern: "from src\\.risk\\.sizing import"
---

<objective>
Create the `src/risk/` package from scratch with three pure-math modules: disagreement scoring (RISK-01), CPPI drawdown multiplier (RISK-02), and fractional Kelly sizing helper (RISK-03). Create corresponding test files.

Purpose: These are the deterministic mathematical building blocks for the hybrid decision engine. All functions are stateless, stdlib-only, and produce float results in well-defined ranges. No agent code is changed. No external dependencies are added.

Output: `src/risk/__init__.py`, `src/risk/disagreement.py`, `src/risk/drawdown_guardrail.py`, `src/risk/sizing.py`, plus four test files under `tests/risk/`.
</objective>

<execution_context>
@C:/Users/click/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/click/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-schemas/01-CONTEXT.md
@.planning/phases/01-foundation-schemas/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/risk package — disagreement module (RISK-01)</name>
  <files>src/risk/__init__.py, src/risk/disagreement.py, tests/risk/__init__.py, tests/risk/test_disagreement.py</files>
  <read_first>
    - .planning/phases/01-foundation-schemas/01-CONTEXT.md — locked decisions D-01, D-02, D-03 define exact semantics
    - .planning/phases/01-foundation-schemas/01-RESEARCH.md — Pattern 1 shows the complete reference implementation; Pitfall 3 explains population std requirement
  </read_first>
  <behavior>
    - Test 1 (D-01, D-02): `compute_disagreement_score([1, 1, 1])` == 0.0 (unanimous buy = no disagreement)
    - Test 2 (D-02): `compute_disagreement_score([1, 1, -1, -1])` == 1.0 (50/50 buy/sell = maximum disagreement)
    - Test 3 (D-01, D-02): `compute_disagreement_score([-1, -1, -1])` == 0.0 (unanimous sell = no disagreement)
    - Test 4 (D-03): `compute_disagreement_score([1, None, -1])` — None is imputed as 0; result == same as `compute_disagreement_score([1, 0, -1])`
    - Test 5 (D-03): `compute_disagreement_score([None, None])` == 0.0 (all imputed as 0 = unanimous neutral)
    - Test 6: `compute_disagreement_score([1])` == 0.0 (single analyst = no disagreement possible)
    - Test 7: `compute_disagreement_score([])` == 0.0 (empty list = no disagreement)
    - Test 8 (D-02): `compute_disagreement_multiplier([1, 1, 1])` == 1.0 (1.0 - 0.0)
    - Test 9 (D-02): `compute_disagreement_multiplier([1, 1, -1, -1])` == 0.0 (1.0 - 1.0)
    - Test 10: result of `compute_disagreement_score` is always in [0.0, 1.0] for any input of ints and Nones in {-1, 0, 1, None}
    - Test 11: result of `compute_disagreement_multiplier` is always in [0.0, 1.0]
  </behavior>
  <action>
    Create four files:

    FILE 1 — `src/risk/__init__.py`: Empty file (or a single-line docstring `"""src.risk — deterministic risk math utilities."""`). No imports, no re-exports.

    FILE 2 — `src/risk/disagreement.py`:
    - Module docstring: "Deterministic signal disagreement score and multiplier. RISK-01. Source: CONTEXT.md D-01, D-02, D-03."
    - Imports: `from math import sqrt` only (stdlib).
    - Function `compute_disagreement_score(stances: list[int | None]) -> float`:
      - Impute None as 0: `values = [s if s is not None else 0 for s in stances]`
      - If len(values) < 2: return 0.0
      - Compute population mean: `mean = sum(values) / len(values)`
      - Compute population variance (divide by N, not N-1): `variance = sum((v - mean) ** 2 for v in values) / len(values)`
      - Return `min(sqrt(variance), 1.0)` — clamped to [0.0, 1.0]; min() ensures no value > 1.0 regardless of edge inputs
    - Function `compute_disagreement_multiplier(stances: list[int | None]) -> float`:
      - Returns `1.0 - compute_disagreement_score(stances)` — per D-02

    FILE 3 — `tests/risk/__init__.py`: Empty file.

    FILE 4 — `tests/risk/test_disagreement.py`:
    - Import: `from src.risk.disagreement import compute_disagreement_score, compute_disagreement_multiplier`
    - Class `TestComputeDisagreementScore` with test methods covering all 11 behaviors listed above
    - Use `pytest.approx` for float equality assertions (e.g. `assert result == pytest.approx(0.0)`)
    - Include a parametrize test for the [0.0, 1.0] range property across varied inputs

    CRITICAL (Pitfall 3): Use POPULATION std (divide by N, not N-1). For `[1, 1, -1, -1]`:
    - mean = 0.0; variance = (1+1+1+1)/4 = 1.0; sqrt(1.0) = 1.0 — correct max
    - With sample std (N-1): variance = 4/3 > 1.0; sqrt > 1.0 — WRONG
  </action>
  <verify>
    <automated>cd "C:/workspaces/ai-hedge-fund-forked" && poetry run pytest tests/risk/test_disagreement.py -v --no-cov 2>&1 | tail -20</automated>
  </verify>
  <done>pytest reports all tests in test_disagreement.py collected and passing. No import errors. `grep "from math import" src/risk/disagreement.py` shows only stdlib import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CPPI drawdown guardrail module (RISK-02)</name>
  <files>src/risk/drawdown_guardrail.py, tests/risk/test_drawdown_guardrail.py</files>
  <read_first>
    - .planning/phases/01-foundation-schemas/01-CONTEXT.md — locked decisions D-04, D-05, D-06, D-07 define exact CPPI semantics
    - .planning/phases/01-foundation-schemas/01-RESEARCH.md — Pattern 3 shows the complete reference implementation; Pitfall 4 explains zero-division guard
  </read_first>
  <behavior>
    - Test 1 (D-04): `compute_cppi_floor(peak_value=100_000.0, max_drawdown_limit=0.20)` == 80_000.0 (floor = peak * (1 - limit))
    - Test 2 (D-05): `compute_cppi_multiplier(portfolio_value=100_000.0, peak_value=100_000.0, max_drawdown_limit=0.20)` == 1.0 (full cushion)
    - Test 3 (D-05): `compute_cppi_multiplier(portfolio_value=80_000.0, peak_value=100_000.0, max_drawdown_limit=0.20)` == 0.0 (at floor = zero cushion)
    - Test 4 (D-05): `compute_cppi_multiplier(portfolio_value=90_000.0, peak_value=100_000.0, max_drawdown_limit=0.20)` == 0.5 (cushion=10k, denominator=20k)
    - Test 5 (D-05): `compute_cppi_multiplier(portfolio_value=79_000.0, peak_value=100_000.0, max_drawdown_limit=0.20)` == 0.0 (below floor — cushion negative, clamp to 0)
    - Test 6 (D-05): multiplier never exceeds 1.0 — test with portfolio_value > peak_value returns 1.0 (capped)
    - Test 7 (Pitfall 4): `compute_cppi_multiplier(portfolio_value=0.0, peak_value=0.0, max_drawdown_limit=0.20)` == 1.0 (no peak yet; return neutral)
    - Test 8 (D-06): default max_drawdown_limit parameter is 0.20 — calling `compute_cppi_multiplier(100_000.0, 100_000.0)` without the third arg returns 1.0
    - Test 9: result is always in [0.0, 1.0]
  </behavior>
  <action>
    Create two files:

    FILE 1 — `src/risk/drawdown_guardrail.py`:
    - Module docstring: "CPPI trailing peak floor and risk multiplier. RISK-02. Source: CONTEXT.md D-04, D-05, D-06."
    - No imports needed (pure arithmetic).
    - Function `compute_cppi_floor(peak_value: float, max_drawdown_limit: float = 0.20) -> float`:
      - Returns `peak_value * (1.0 - max_drawdown_limit)` — per D-04
    - Function `compute_cppi_multiplier(portfolio_value: float, peak_value: float, max_drawdown_limit: float = 0.20) -> float`:
      - Guard: if `peak_value <= 0.0` return `1.0` — per Pitfall 4 (no peak established yet; neutral multiplier)
      - Guard: if `max_drawdown_limit <= 0.0` return `1.0` (degenerate config; no protection active)
      - `floor = peak_value * (1.0 - max_drawdown_limit)` — per D-04
      - `cushion = portfolio_value - floor`
      - If `cushion <= 0.0`: return `0.0` (at or below floor = full suppression)
      - `denominator = max_drawdown_limit * peak_value`
      - Return `min(cushion / denominator, 1.0)` — per D-05; capped at 1.0

    FILE 2 — `tests/risk/test_drawdown_guardrail.py`:
    - Import: `from src.risk.drawdown_guardrail import compute_cppi_floor, compute_cppi_multiplier`
    - Class `TestComputeCppiFloor` with test for D-04 floor calculation
    - Class `TestComputeCppiMultiplier` covering all behaviors listed above
    - Use `pytest.approx` for float equality assertions

    IMPORTANT: D-07 says peak_value is PERSISTED by the caller (risk_manager, Phase 3), not by this module. This module is stateless — it receives peak_value as a parameter. Do not add any global state or peak-tracking logic.
  </action>
  <verify>
    <automated>cd "C:/workspaces/ai-hedge-fund-forked" && poetry run pytest tests/risk/test_drawdown_guardrail.py -v --no-cov 2>&1 | tail -20</automated>
  </verify>
  <done>pytest reports all tests in test_drawdown_guardrail.py collected and passing. `grep "import" src/risk/drawdown_guardrail.py` returns empty or module-level docstring only (no imports — pure arithmetic).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create fractional Kelly sizing helper module (RISK-03)</name>
  <files>src/risk/sizing.py, tests/risk/test_sizing.py</files>
  <read_first>
    - .planning/phases/01-foundation-schemas/01-CONTEXT.md — locked decisions D-08, D-09, D-10, D-11
    - .planning/phases/01-foundation-schemas/01-RESEARCH.md — Pattern 4 shows the complete reference implementation; Pitfall 5 explains the disabled=1.0 (not 0.0) rule
  </read_first>
  <behavior>
    - Test 1 (D-10): `fractional_kelly(win_probability=0.6, win_loss_ratio=2.0)` == 1.0 (disabled by default — neutral multiplier)
    - Test 2 (D-10): `fractional_kelly(win_probability=0.6, win_loss_ratio=2.0, enabled=False)` == 1.0 (explicitly disabled)
    - Test 3 (D-09): `fractional_kelly(win_probability=0.6, win_loss_ratio=2.0, enabled=True)`:
      - Full Kelly = (0.6*2.0 - 0.4) / 2.0 = (1.2 - 0.4) / 2.0 = 0.8 / 2.0 = 0.4
      - Quarter Kelly = 0.4 * 0.25 = 0.1
      - Result == pytest.approx(0.1)
    - Test 4 (D-09): Result when enabled is always <= 0.25 (hard cap)
    - Test 5 (D-11): `fractional_kelly(win_probability=0.1, win_loss_ratio=0.5, enabled=True)`:
      - Full Kelly = (0.1*0.5 - 0.9) / 0.5 = (0.05 - 0.9) / 0.5 = -0.85 / 0.5 = -1.7
      - Quarter Kelly = -1.7 * 0.25 = -0.425; floored to 0.0
      - Result == pytest.approx(0.0)
    - Test 6 (D-09): `fractional_kelly(win_probability=0.99, win_loss_ratio=100.0, enabled=True)`:
      - Full Kelly would be very large; Quarter Kelly capped at 0.25
      - Result == pytest.approx(0.25)
    - Test 7: result when enabled is always in [0.0, 0.25] for any valid p in (0,1) and b > 0
    - Test 8 (D-10, Pitfall 5): disabled result is 1.0, NOT 0.0 — a zero multiplier would suppress all trades
  </behavior>
  <action>
    Create two files:

    FILE 1 — `src/risk/sizing.py`:
    - Module docstring: "Fractional Kelly position sizing helper. RISK-03. Source: CONTEXT.md D-08, D-09, D-10, D-11."
    - No imports needed (pure arithmetic).
    - Function `fractional_kelly(win_probability: float, win_loss_ratio: float, enabled: bool = False) -> float`:
      - Per D-10: if `not enabled`: return `1.0` (neutral — no effect on sizing)
      - Per D-08: caller passes win_probability (p) and win_loss_ratio (b) explicitly
      - Compute full Kelly: `q = 1.0 - win_probability`; `full_kelly = (win_probability * win_loss_ratio - q) / win_loss_ratio`
      - Per D-09: `quarter_kelly = full_kelly * 0.25`
      - Per D-11: `quarter_kelly = max(quarter_kelly, 0.0)` — floor at 0 (no negatives)
      - Per D-09: return `min(quarter_kelly, 0.25)` — hard cap at 0.25

    FILE 2 — `tests/risk/test_sizing.py`:
    - Import: `from src.risk.sizing import fractional_kelly`
    - Class `TestFractionalKelly` covering all 8 behaviors above
    - Use `pytest.approx` for float assertions (default tolerance is 1e-6)
    - Include a parametrize test confirming result is in [0.0, 0.25] for varied (p, b) pairs when enabled=True

    CRITICAL (Pitfall 5, D-10): When `enabled=False`, return `1.0` not `0.0`. A return of 0.0 would zero out all position sizing and block all trades — that is NOT the intent of "disabled".
  </action>
  <verify>
    <automated>cd "C:/workspaces/ai-hedge-fund-forked" && poetry run pytest tests/risk/test_sizing.py -v --no-cov 2>&1 | tail -20</automated>
  </verify>
  <done>pytest reports all tests in test_sizing.py collected and passing. `fractional_kelly.__defaults__` shows (False,) confirming the `enabled=False` default. No import errors.</done>
</task>

<task type="auto">
  <name>Task 4: Full regression — verify complete test suite passes</name>
  <files></files>
  <read_first>
    - No file read needed; pure verification step
  </read_first>
  <action>
    Run the full pytest suite. This verifies that:
    1. All three new risk modules import cleanly within the project's Python environment
    2. No existing test is broken by the new src/risk/ package additions
    3. Coverage gate (42%) still passes — new files add tested code, so coverage should improve or stay the same

    If failures occur, read the error output carefully: likely an import path issue (e.g. missing tests/risk/__init__.py) or a math edge case. Fix the specific failing test or source file; do not alter passing tests.
  </action>
  <verify>
    <automated>cd "C:/workspaces/ai-hedge-fund-forked" && poetry run pytest -q 2>&1 | tail -15</automated>
  </verify>
  <done>Full suite passes: 0 failures, 0 errors. Passed count >= 417 (baseline) plus new risk tests. Coverage remains >= 42%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → risk functions | All risk functions receive scalar/list inputs from caller agents; inputs are not validated beyond Python type hints — callers are trusted (same process, same codebase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | compute_cppi_multiplier division | accept | Guards for peak_value <= 0 and max_drawdown_limit <= 0 prevent ZeroDivisionError; cushion <= 0 returns 0.0; result clamped to [0.0, 1.0] |
| T-02-02 | Tampering | fractional_kelly negative result | accept | D-11 and max(quarter_kelly, 0.0) floor prevent negative multipliers reaching portfolio sizing |
| T-02-03 | Denial of Service | Very large stances list in disagreement | accept | O(N) stdlib computation; no external I/O; no stack recursion; not a concern for expected agent counts (18 analysts max) |
| T-02-SC | Tampering | npm/pip installs | accept | No new packages installed; stdlib math only |
</threat_model>

<verification>
1. `ls src/risk/` shows __init__.py, disagreement.py, drawdown_guardrail.py, sizing.py
2. `ls tests/risk/` shows __init__.py, test_disagreement.py, test_drawdown_guardrail.py, test_sizing.py
3. `grep "import numpy\|import pandas" src/risk/*.py` returns nothing (stdlib-only confirmed)
4. `poetry run pytest tests/risk/ -q --no-cov` — all pass
5. `poetry run pytest tests/schemas/ tests/risk/ -q --no-cov` — combined 41+ schema + all risk tests pass
6. `poetry run pytest -q` — full suite green, >= 417 + new tests, coverage >= 42%
</verification>

<success_criteria>
- RISK-01: compute_disagreement_score in [0.0, 1.0]; 0.0 for unanimous; 1.0 for 50/50 buy/sell; None imputed as 0
- RISK-02: compute_cppi_multiplier in [0.0, 1.0]; 0.0 at/below floor; 1.0 at peak; 1.0 when peak_value=0 (no peak yet)
- RISK-03: fractional_kelly returns 1.0 when disabled; Quarter Kelly in [0.0, 0.25] when enabled; floored at 0.0; capped at 0.25
- All risk functions: pure Python stdlib, no numpy/pandas, no LLM, no I/O, stateless
- Full test suite: 0 regressions, coverage >= 42%
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File | Notes |
|--------|------|------|-------|
| `src/risk/` | package | `src/risk/__init__.py` | New package; empty init |
| `compute_disagreement_score` | function | `src/risk/disagreement.py` | `(stances: list[int \| None]) -> float`; D-01/D-02/D-03 |
| `compute_disagreement_multiplier` | function | `src/risk/disagreement.py` | `(stances: list[int \| None]) -> float`; returns `1.0 - score` |
| `compute_cppi_floor` | function | `src/risk/drawdown_guardrail.py` | `(peak_value: float, max_drawdown_limit: float = 0.20) -> float`; D-04 |
| `compute_cppi_multiplier` | function | `src/risk/drawdown_guardrail.py` | `(portfolio_value: float, peak_value: float, max_drawdown_limit: float = 0.20) -> float`; D-05/D-06 |
| `fractional_kelly` | function | `src/risk/sizing.py` | `(win_probability: float, win_loss_ratio: float, enabled: bool = False) -> float`; D-08/D-09/D-10/D-11 |
| `tests/risk/` | package | `tests/risk/__init__.py` | New test package |
| `TestComputeDisagreementScore` | test class | `tests/risk/test_disagreement.py` | 11+ tests; RISK-01 |
| `TestComputeCppiFloor` | test class | `tests/risk/test_drawdown_guardrail.py` | RISK-02 floor |
| `TestComputeCppiMultiplier` | test class | `tests/risk/test_drawdown_guardrail.py` | RISK-02 multiplier |
| `TestFractionalKelly` | test class | `tests/risk/test_sizing.py` | 8+ tests; RISK-03 |

<output>
Create `.planning/phases/01-foundation-schemas/01-02-SUMMARY.md` when done.
</output>
